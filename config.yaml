# Serving parameters (fixture: stable values for extraction tests)
service:
  name: qwen-chat-demo

model:
  id: "Qwen/Qwen2-7B-Instruct"
  trust_remote_code: false

vllm:
  tensor_parallel_size: 1
  max_model_len: 16384
  gpu_memory_utilization: 0.92
  dtype: bfloat16

server:
  host: "0.0.0.0"
  port: 8000
